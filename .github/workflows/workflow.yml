name: primer workflow 

on: [push]

jobs: 
    build: 
        runs-on: ubuntu-latest
    
        steps:
        - name: echo a string
          run: cp workflow.yml Laura-adm.-sist.-y-redes/.github/workflows/copiaworkflow.yml
                
              