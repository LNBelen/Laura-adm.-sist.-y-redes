name: primer workflow 

on: [push]

jobs: 
    build: 
        runs-on: ubuntu-latest
    
        steps:
        - name: echo a string
          run: | 
                echo "hola mundo"
                pwd
                cp workflow.yml /home/runner/work/Laura-adm.-sist.-y-redes/workflow2.yml 